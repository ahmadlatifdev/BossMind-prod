const Sentry = require('@sentry/node'); Sentry.init({ dsn: 'https://216ffa4485f88b8250100eee059110b5@o4511260643229696.ingest.us.sentry.io/4511260644474880' }); async function analyzeWithDeepSeek(error){ const key = process.env.DEEPSEEK_API_KEY; if(!key){ console.log('DEEPSEEK_API_KEY missing'); return; } const response = await fetch('https://api.deepseek.com/chat/completions',{ method:'POST', headers:{ 'Content-Type':'application/json','Authorization':'Bearer '+key }, body: JSON.stringify({ model:'deepseek-chat', messages:[{role:'system',content:'You are BossMind incident repair AI. Analyze the error, identify root cause, and return exact fix plan.'},{role:'user',content:error.stack || error.message}] }) }); const data = await response.json(); console.log('DEEPSEEK ROOT CAUSE ANALYSIS:', JSON.stringify(data)); } async function run(){ const error = new Error('BOSSMIND INCIDENT TEST FOR DEEPSEEK'); Sentry.captureException(error); await Sentry.flush(5000); await analyzeWithDeepSeek(error); console.log('SENTRY + DEEPSEEK STEP COMPLETE'); setInterval(()=>console.log('bossmind incident worker alive'),60000); } run();
