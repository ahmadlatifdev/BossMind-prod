require('dotenv').config({ path: __dirname + '/.env' }); const Sentry = require('@sentry/node'); Sentry.init({ dsn: process.env.SENTRY_DSN }); async function run(){ Sentry.captureException(new Error('SENTRY FINAL SUCCESS FROM ENV FILE')); await Sentry.flush(5000); console.log('FLUSH DONE'); setInterval(()=>console.log('BossMind worker alive'),60000); } run();
